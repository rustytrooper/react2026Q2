import type { ReactNode } from 'react';

export function About(): ReactNode {
  return (
    <p>
      This is a project, that is developing at the RS React course. Each weak
      there are new features in here, so let's keep in touch
    </p>
  );
}
